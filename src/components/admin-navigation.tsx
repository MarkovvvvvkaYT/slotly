import Link from "next/link";
const links = [["/admin", "Обзор"], ["/admin/bookings", "Заявки"], ["/admin/services", "Услуги"], ["/admin/profile", "Профиль"]] as const;
export function AdminNavigation({ current }: { current: string }) { return <nav aria-label="Разделы панели" className="mb-8 flex gap-2 overflow-x-auto border-b border-[var(--line)] pb-4">{links.map(([href,label])=><Link key={href} href={href} aria-current={current === href ? "page" : undefined} className={`whitespace-nowrap rounded-full border px-4 py-2 text-sm font-bold transition ${current===href?"border-[var(--brand)] bg-[var(--brand)] text-[var(--paper)]":"border-[var(--line)] bg-[var(--card)] text-[var(--muted)] hover:border-[var(--brand)] hover:text-[var(--brand)]"}`}>{label}</Link>)}</nav>; }
